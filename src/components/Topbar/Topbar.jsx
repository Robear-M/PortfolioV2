import React from "react";
import { getImageUrl } from "../../utils";
import styles from "./Topbar.module.css"

export const Topbar = () => {
    return (
            <h1>Topbar Section</h1>
        );
    }